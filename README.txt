Starting off by creating a model in Neo4j that store all the route information 
and expose that as REST APIs. Once we are familiar with graph modeling, will 
include the other elements like bus etc.

Some of the libraries/tools uses are:-
1) Neo4j
2) Spring Data Graph
3) CXF for REST and
4) Gradle
